TESTS_ENVIRONMENT = PYTHONPATH=${srcdir}/../pyrpm:@PY_PYTHONPATH@
TESTS = yumconfigtest
EXTRA_DIST = $(TESTS) yum-test.tgz

CLEANFILES := .coverage stdout stderr $(notdir $(wildcard *,cover)) $(notdir $(wildcard *~)) $(notdir $(wildcard *\#))


